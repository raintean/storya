{
  "name": "storya-edge-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "benchmark": "tsx scripts/benchmark.ts",
    "benchmark:connections": "tsx scripts/connection-storm-benchmark.ts",
    "build": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p scripts/tsconfig.json --noEmit"
  },
  "dependencies": {
    "storya-protocol": "workspace:*"
  },
  "devDependencies": {
    "storya-transport": "workspace:*",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "wrangler": "catalog:"
  }
}
